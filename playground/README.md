# easy-table playground

Run from the repository root:

```bash
pnpm playground:dev
```

Available scripts:

- `pnpm playground:dev` - start the Vite dev server.
- `pnpm playground:build` - build a production playground bundle.
- `pnpm playground:preview` - preview the production build.
